import React, { Fragment } from 'react';
import '../scss/index.scss';

const Toast = () => {
    return (
        <Fragment>
            <div className="a">
                <p>Not found city</p>
            </div>
        </Fragment>
    );
};

export default Toast;
